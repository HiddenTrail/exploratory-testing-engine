import os

# Content for the markdown documentation file
readme_content = """# FYI / TODO: Scientific Challenger Agent Model

This document outlines the architecture, epistemic directives, and AWS Bedrock integration blueprints for the **Scientific Challenger Agent**. This agent functions as a formal, adversarial academic opponent to stress-test theories generated by the tester agent without offering testing implementation advice.

---

## 🧠 Core Philosophy: Popperian Falsification
Unlike standard QA reviewers that suggest test execution steps, this agent operates purely on **scientific falsification** and **epistemic peer review**. 
* **Target:** It evaluates the *logic* of the theory, not the *mechanics* of the test execution.
* **Tone:** Peer-to-peer critique, challenging assumptions directly (e.g., *"You assume mechanism X, but have you decoupled that from potential race conditions in layer Y?"*).
* **Constraint:** Strictly forbidden from outputting test scenarios, payload designs, or test cases.

---

## 📋 Architectural Specifications

### 1. System Prompt Blueprint
When executing direct API calls against **Anthropic Claude on AWS Bedrock**, feed the following string into the `system` block of the API request payload:

```text
You are an elite, adversarial scientific opponent (thesis challenger) specializing in formal epistemic peer review. Your sole objective is to analyze the logic, unstated assumptions, and causal claims of the provided theory.

OPERATIONAL DIRECTIVES:
1. Focus entirely on structural, logical, and conceptual vulnerabilities.
2. Identify implicit or unstated assumptions (axioms) that the theory relies upon.
3. Assess the epistemic strength of the theory based on formal logic.
4. Provide alternative, competing theories that could explain the exact same structural phenomena.
5. CRITICAL: Do NOT suggest specific test cases, implementation steps, or QA methodologies. Address the theory, not the testing mechanism.
6. Speak directly to the tester agent as an analytical peer (e.g., 'You assume X, but have you accounted for the possibility that...').
```

### 2. Structured Output via Anthropic Tool Use (JSON Schema)
To enforce structured, programmatic evaluation logs from Bedrock, inject the following definition into the `tools` array of your Anthropic API payload. Set `tool_choice={"type": "any"}` to compel a structured response.

```json
{
    "name": "generate_epistemic_critique",
    "description": "Executes a formal scientific challenge against a generated theory, mapping logical gaps and alternative hypotheses.",
    "input_schema": {
        "type": "object",
        "properties": {
            "theory_assessment": {
                "type": "string",
                "enum": ["STRONG", "MODERATE", "WEAK"],
                "description": "The epistemic rating of the theory based on its internal consistency and logical resilience."
            },
            "conceptual_gaps": {
                "type": "array",
                "items": { "type": "string" },
                "description": "List of holes, hidden assumptions, or logical leaps identified in the reasoning chain using 'You assume X...' format."
            },
            "alternative_hypotheses": {
                "type": "array",
                "items": { "type": "string" },
                "description": "Parallel theoretical frameworks or mechanisms that explain the observed patterns equally well."
            },
            "evidentiary_deficiencies": {
                "type": "array",
                "items": { "type": "string" },
                "description": "What abstract, empirical, or structural proof the theory lacks to transition from a correlation to a robust causal law."
            }
        },
        "required": ["theory_assessment", "conceptual_gaps", "alternative_hypotheses", "evidentiary_deficiencies"]
    }
}
```

---

## 🛠️ TODO / Implementation Steps

- [ ] **Bedrock Payload Integration:** Add the `system` prompt string into your local boto3/Bedrock client payload manager.
- [ ] **Tool Enforcement:** Inject the `generate_epistemic_critique` tool schema into your message request structure.
- [ ] **Temperature Tuning:** Set the challenger agent's temperature explicitly low (`0.1` to `0.2`) to eliminate creative fluff and ensure rigid, literal, and strict analytical evaluation.
- [ ] **State Bridge Pattern:** Ensure the output of the testing agent is passed to this challenger as a simple text pair: `Observation` + `Theory`, keeping context tokens tightly constrained to the hypothesis itself.
"""

# Ensure directory exists and write the file
os.makedirs("generated", exist_ok=True)
with open("generated/SCIENTIFIC_CHALLENGER_README.md", "w", encoding="utf-8") as f:
    f.write(readme_content)
